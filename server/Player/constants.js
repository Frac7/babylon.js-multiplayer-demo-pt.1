 // Ground width (and height) is 128, its default center is (0,0,0)
 // So the position equals to the default center and its min and max coordinates are width / 2
 // One coordinate positive and one coordinate negative
const MAX_COORDINATE = 64;
const MIN_COORDINATE = -64;

module.exports = {
  MAX_COORDINATE,
  MIN_COORDINATE
};